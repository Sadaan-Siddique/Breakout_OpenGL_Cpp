#pragma once // #pragma once is a safety feature that prevents the compiler from accidentally copying this header file twice into the same file.

// These are the "promises" that these functions exist somewhere else.
void windowInitialize();
void mainRenderingLoop();
void terminateWindow();